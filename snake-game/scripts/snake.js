export let snake = {
  body: [10],
  speed: 1,
};

export const drawSnake = (gameBoard) => {
  // create snake body square element
  let snakeBody = document.createElement("div");
  snakeBody.classList.add("snake-piece");
};
